# Answers completely from my mind

# Questions 
1. What is Node.js?
2. Is Node.js a programming language?
3. Is Node.js a framework?
4. What command do you use to run a JavaScript file?

# Answer
1. As far as I learnt, Node.js is a js runtime. So, its a bit different from js. Like js can run script in the browser but not in backend. Node creates an environment so that js can run in the backend or server
2. Hmm, idk. I think its not a programming language cause because it does not use different language. Its uses Js.
3. No, Node.js is not a framework. Cause I think, frameworks work in the browser not really in the backend or server. So, if Node.js would have been a framework then it wouldn't be used to run a server. Like it creates an environment where Js can be used to write or handel a sever. 
4. To run a Js file from the command or teminal pannel. I wrote as I structed the files in my project. As my hello.js was in lesson-02/experiments. I first changed the directory of the project to that, `cd ./lesson-02/experiments` then I wrote `node hello.js`. But mainly we write `node hello.js`

# Reflection

Today I understood that browsers don't block features like the camera or location because they are dangerous by themselves. Instead, browsers ask for my permission. This creates a balance between usability and security.